gem 'rails', '~> 5.1.2'

group :production do
  # pg as production database for Active Record
  gem 'pg'
  gem 'rails_12factor'
end

group :development do
  # sqlite3 as dev database for Active Record
  gem 'sqlite3'
end

 # Puma as app server
gem 'puma', '~> 3.0'
 # Sass-rails for SCSS preprocessing
gem 'sass-rails', '~> 5.0'
 # Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
 # JavaScript library
gem 'jquery-rails'
 # Turbolinks for speeding up web application.
gem 'turbolinks', '~> 5'
 # JSON API Builder
gem 'jbuilder', '~> 2.5'
gem 'thor', '0.19.1'

group :development do
  gem 'listen', '~> 3.0.5'
end

group :development, :test do
 gem 'rspec-rails', '~> 3.0'
 gem 'rails-controller-testing'
 gem 'shoulda'
 gem 'factory_girl_rails', '~> 4.0'
end

gem 'bootstrap-sass'
gem 'bcrypt'
